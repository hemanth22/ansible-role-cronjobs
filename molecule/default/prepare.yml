---
- name: Install cronie dependencies
  hosts: localhost
  become: true
  gather_facts: true
  tasks:
    - name: Install dependencies
      ansible.builtin.package:
        name: "{{ 'crond' if ansible_facts['os_family'] == 'RedHat' else 'cron' }}"
        state: present
